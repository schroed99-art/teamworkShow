<!--
  Splash-screen icon for the Android 12+ SplashScreen API.
  Real Teamwork Show emblem (extracted from splashscreen_v3) on a black ground
  that matches the splash window; safe-zone padding is baked into the asset, so
  the system's circular mask never clips the figures.
-->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/splash_logo"
    android:gravity="fill" />
